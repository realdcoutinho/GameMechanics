using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModeManager : MonoBehaviour
{
    private bool _currentMode = true;
    GameObject _gameObject;
    public void Manage (bool mode)
    {
        if(_currentMode)
        {
            if(!mode)
            {
                
            }
        }
    }
}
